import Information from "@/components/Dashboard/profile/Information";

function Profile() {
  // const user = useStore((state: any) => state.userData);
  return (
    <div className="">
      <Information />
    </div>
  );
}

export default Profile;
