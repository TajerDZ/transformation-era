import Page from "@/components/Dashboard/store/web-design/Page";

function WebDesign() {
  // const user = useStore((state: any) => state.userData);
  return (
    <div className="">
      <Page />
    </div>
  );
}

export default WebDesign;
