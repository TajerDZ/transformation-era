import Main from "@/components/Dashboard/products/Main";

function Products() {
  // const user = useStore((state: any) => state.userData);
  return (
    <div className="">
      <Main />
    </div>
  );
}

export default Products;
